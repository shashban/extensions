# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger: none
jobs:
- job: DeployDev  

  variables:
    - group: Test

  steps:
  - script: "echo $(branch_Ring0)"

- job: DeployQA 

  variables:
    - group: AllowedBranchesPerEnvironment

  steps:
  - script: "echo $(branch_Ring0)"
